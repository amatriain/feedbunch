[
  {
    "id": "all",
    "title": "<%= t 'feeds.index.all_feeds' %>",
    "unread_entries": <%= user.unread_entries %>
  }
  <% folders.each do |folder| %>

    ,
    {
      "id": <%= folder.id %>,
      "title": "<%= folder.title %>",
      "unread_entries": <%= folder.unread_entries %>
    }

  <% end %>
]