[
{
  "id": "all",
  "title": "<%= t 'feeds.index.all_feeds' %>",
  "unread_entries": <%= user.folder_unread_count 'all' %>
}
  <% folders.each do |folder| %>
,
<%= render partial: 'folder', locals: {folder: folder} %>
  <% end %>
]