<% self.formats = [:html] %>

<%
  if user.data_import.present?
    status = user.data_import.status
  else
    status = 'NONE'
  end
%>

{
  "status": "<%= status %>",
  "status_html": <%= render(partial: 'subscriptions_data/import', locals: {user: user}, layout: false).to_json.html_safe %>
}