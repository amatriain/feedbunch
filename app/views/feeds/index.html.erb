<div class="row-fluid">

  <div class="span3">
    <%= render 'sidebar' %>
  </div>

  <div class="span9">
    <%# TODO make the "feed" variable dynamically use the currently selected feed %>
    <% feed = Feed.first %>
    <% if feed.present? %>
      <h2><%= feed.title %></h2>
    <% end %>
  </div>

</div>
