<li>
  <a data-toggle="collapse" data-target="#<%= folder_id %>" class="menu-level1">
    <i class="folder icon-folder-close-alt"></i> <%= title %><i class="arrow icon-chevron-right pull-right"></i>
  </a>
  <ul class="nav nav-list collapse" id="<%= folder_id %>">
    <% feeds.each do |feed| %>
      <li>
        <a data-feed-id="<%= feed.id %>" data-feed-title="<%= feed.title %>" data-feed-url="<%= feed.url %>">
          <i class="icon-rss"></i> <%= feed.title %> <i class="icon-spinner hidden"></i>
        </a>
      </li>
    <% end %>
  </ul>
</li>