<div class="row top-alert" ng-cloak>
  <div class="col-md-12 text-center">

    <%= render 'read/alerts' %>
    <%= render 'read/data_import_alerts' %>

  </div>
</div>

<%= render 'read/popups/subscribe_form' %>
<%= render 'read/popups/unsubscribe_confirm' %>
<%= render 'read/popups/new_folder_form' %>

<div class="row">

  <div id="sidebar-column" class="col-md-3 pull-left">
    <%= render 'read/sidebar/sidebar' %>
  </div>

  <div class="col-md-9 pull-right" ng-cloak>

    <div ng-show="current_feed==null && current_folder==null && !loading_entries_busy">
      <%= render 'start' %>
    </div>

    <div ng-show="current_feed || current_folder">
      <div id="feed-title">
        <strong class="lead">
          <a href="{{current_feed.url}}" target="_blank">{{current_feed.title || current_folder.title}}</a>
        </strong>
        <hr>
      </div>

      <%= render 'entries' %>

    </div>

    <div id="loading" class="col-md-offset-1" ng-show="loading_entries_busy">
      <i class="fa fa-spinner fa-spin"></i> <%= t 'feeds.index.loading' %>
    </div>

    <%= render 'layouts/footer' %>

  </div>
</div>