<!DOCTYPE html>
<html>
<head>
  <title>Feedbunch</title>
  <%= favicon_link_tag %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= render 'layouts/navbar' %>

<%= render 'data_imports/popups/import_subscriptions_form' %>

<div class="span8 offset2 top-alert">

  <%= render 'layouts/alerts' %>
  <%= render 'devise/alerts' %>

</div>

<div class="row-fluid page-header">
  <div class="span10 offset1">
    <h1>

      <%=
          link_to root_url do
            image_tag asset_path('feed128x128.png'), alt: 'feedbunch'
          end
      %>

      <%= yield :heading %>
    </h1>
  </div>
</div>

<div class="row-fluid">
  <div class="span2 offset9">
    <div class="btn-group">
      <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
        <i class="icon-flag"></i>
        <%= t 'devise.views.language' %>
        <span class="caret"></span>
      </a>
      <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
        <li>
          <%= link_to locale: 'en' do %>
            <%= image_tag asset_path('flags/us.png') %>
            English
          <% end %>
        </li>
        <li>
          <%= link_to locale: 'es' do %>
            <%= image_tag asset_path('flags/es.png') %>
            Español
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</div>

<br>

<%= yield %>

<%= javascript_include_tag "application" %>

</body>
</html>
