{
  "job_statuses": [
    <% job_statuses.each do |job_status| %>
    <%= render partial: 'api/refresh_feed_job_statuses/job_status', locals: {job_status: job_status, user: user} %>
    <% if job_status != job_statuses.last %>
    ,
    <% end %>
    <% end %>
  ]
}